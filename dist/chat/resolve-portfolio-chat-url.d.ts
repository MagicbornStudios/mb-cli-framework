/**
 * Resolve the portfolio Site Copilot chat endpoint for terminal clients.
 * Same route the web UI uses: `POST /api/chat` with `{ messages }`.
 */
export declare function resolvePortfolioChatApiUrl(): string;
